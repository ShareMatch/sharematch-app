# PoC Requirements: SVG to 3D Premium Images Pipeline
# Install with: pip install -r requirements-poc.txt

# Core dependencies
requests>=2.28.0
Pillow>=10.0.0
boto3>=1.28.0
python-dotenv>=1.0.0

# XML parsing (built-in with Python 3.8+)
# No additional dependencies needed for xml.etree.ElementTree

# Optional: For better image processing
# numpy>=1.24.0  # If you need advanced image manipulation
# opencv-python>=4.8.0  # If you need computer vision features
